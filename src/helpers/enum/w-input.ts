export enum Types {
  TEXT = 'text',
  DATE = 'date',
  EMAIL = 'email',
  FILE = 'file',
  URL = 'url',
  NUMBER = 'number',
  PASSWORD = 'password',
}

export enum Modes {
  NONE = 'none',
  NUMERIC = 'numeric',
  TEL = 'tel',
  DECIMAL = 'decimal',
  EMAIL = 'email',
  URL = 'url',
  SEARCH = 'search',
}

export enum Events {
  INPUT = 'input',
  BLUR = 'blur',
  FOCUS = 'focus',
  ENTER = 'enter',
  KEYPRESS = 'keypress',
  MOUSE_ENTER = 'mouse-enter',
  MOUSE_LEAVE = 'mouse-leave',
  SET_IS_VALID = 'set-is-valid',
}
